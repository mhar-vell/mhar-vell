# Recoloured icons

`*-white.svg` are [devicon](https://github.com/devicons/devicon) icons
(MIT) repainted white by `tools/whiten_icon.py`. The originals are nearly
black, which leaves them invisible on a dark profile; the README picks
the white copy through `<picture>` and keeps the original for light mode.
